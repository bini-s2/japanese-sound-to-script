# AGENTS.md — Japanese Sound to Script continuation guide

이 문서는 새 ChatGPT/Codex 작업 세션이 이전 대화 맥락 없이 시작해도 **Japanese Sound to Script** 작업을 바로 이어가기 위한 시작점이다.

## 0. 세션 시작 시 가장 먼저 할 일

1. 이 파일과 `docs/CHAT-CONTINUATION-WORKFLOW.md`를 읽는다.
2. 실제 사이트 수정 요청이면 **제품 문서 저장소가 아니라 라이브 소스 저장소**의 최신 파일/SHA를 먼저 가져온다.
3. 사용자가 보낸 최신 스크린샷/요청만 정확히 반영하고, 관련 없는 UI나 기능은 임의로 바꾸지 않는다.
4. 수정 후 GitHub 커밋 → Vercel 배포 상태 확인 → 실제 라이브 링크를 사용자에게 전달한다.

## 1. 저장소 역할

- **제품/기획/핸드오프 문서**
  - `bini-s2/japanese-sound-to-script`
  - 이 문서가 있는 저장소.
- **현재 라이브 웹앱 소스**
  - `bini-s2/readdy-1ff67b` (private)
  - Vercel의 `sound-to-script` 프로젝트와 연결되어 있다.
  - 실제 사이트 UI/검색 로직 수정은 기본적으로 이 저장소에서 진행한다.
- **작업 로그**
  - `bini-s2/Design-log`
  - 사용자가 `/log`, `기록해줘`, `깃허브에 남겨줘`라고 하면 의미 있는 작업을 즉시 기록한다.
- **사용하지 말 것**
  - `bini-s2/readdy-ai-web`은 다른 프로젝트와 겹치므로 Sound to Script 수정에 사용하지 않는다.

## 2. 라이브 주소

- Production: https://sound-to-script.vercel.app/

수정 후에는 “배포됐다”고 말하기 전에 **Vercel commit status가 success인지 확인**한다.

## 3. 사용자와 작업하는 방식

사용자는 빠르게 스크린샷을 보내며 문제를 짚고, 바로 수정 반영되는 흐름을 선호한다.

- 설명만 길게 하지 말고 **수정 가능한 요청이면 실제로 수정**한다.
- 이미 GitHub가 연결되어 있으므로 “직접 푸시할 수 없다”는 식으로 답하지 않는다.
- 간단한 수정은 불필요한 재확인 없이 진행한다.
- 사용자가 표시한 영역 외의 디자인은 함부로 건드리지 않는다.
- 여러 작은 수정은 2~3개씩 묶어서 처리해도 좋다.
- 문제가 재현되면 케이스 하나만 하드코딩하지 말고, 같은 유형의 오류를 줄이는 일반 규칙을 우선한다.
- 다만 알고리즘을 과하게 넓혀 엉뚱한 후보가 늘어나지 않도록 “정확도 > 후보 수”를 우선한다.

## 4. 수정 작업 기본 절차

실제 사이트 수정 요청의 기본 루프:

1. `bini-s2/readdy-1ff67b`에서 대상 파일의 **최신 SHA**를 fetch한다.
2. 필요한 범위만 수정한다.
3. 같은 파일을 연속 수정해야 하면 이전 write 결과의 최신 content SHA 또는 다시 fetch한 SHA를 사용한다.
4. 커밋 메시지는 짧고 목적이 드러나게 남긴다.
5. Vercel commit status가 `success`가 될 때까지 확인한다.
6. 사용자에게 아래 형식으로 회신한다.
   - 무엇을 고쳤는지
   - 배포 성공 여부
   - 라이브 링크
   - 필요하면 테스트해볼 예시 2~5개

## 5. 사용자에게 결과 전달하는 톤

짧고 친근하게. 예:

> 고쳤어 💙  
> 이번엔 `도코에이코 → どこへ行こう`처럼 붙여 쓴 발음을 단어+조사+동사로 먼저 나누도록 수정했어.  
> Vercel 재배포도 성공 확인했어.  
> https://sound-to-script.vercel.app/

주의:
- 실제로 확인하지 않은 배포를 성공했다고 말하지 않는다.
- 링크를 사용자가 바로 누를 수 있게 전달한다.
- 실패하면 실패 원인/다음 조치를 솔직하게 말한다.

## 6. 현재 제품 핵심

Core loop:

`Sound → Script → Meaning → Recall`

현재 주요 플로우:

`Home / Sound Search → Candidate Selection (조건부) → Expression Result → Quick Recall / Deep Dive / Save → My Expressions → Review`

검색 UX의 핵심 전제:
- 사용자는 일본어 철자를 모를 수 있다.
- 한글로 들린 대로 적을 수 있다.
- 띄어쓰기를 빼먹을 수 있다.
- 장음/촉음/유사 자음을 틀릴 수 있다.
- 일본어 + 영어/외래어가 섞인 발음을 기억할 수 있다.
- 문장 전체를 소리만 기억할 수 있다.

따라서 사전 exact match만으로 처리하지 않는다.

## 7. 검색 로직에서 이미 반영된 방향

현재 라이브 소스에는 다음 방향의 검색 개선이 들어가 있다.

- 일본어 사전 기반 넓은 검색
- 한글 발음 → 로마자/가나 후보 변환
- 발음 유사도 기반 후보 정렬
- 긴 고유명사/복합어가 짧은 입력을 포함한다는 이유만으로 상위에 뜨는 현상 감점
- 대명사 + 조사 등 짧은 구문 우선 인식
- 문장형 입력 검색
- 장음/연속 모음/촉음/유사 자음 보정
- “혹시 이 표현을 찾으셨나요?” 추천
- `발음 보정` / `의도 추정` 상태 구분
- 이상한 사전 메타 항목(작품명/인명/고유명사 등) 후순위 처리
- 일본어 원문 + 읽기 + 한국어식 발음 표기
- 붙여 쓴 발음의 단어/조사/동사 분절
  - 예: `도코에이코 → どこへ行こう`

검색 수정 시 **한 케이스를 맞추는 것보다 이 원칙들을 깨지 않는지** 같이 확인한다.

## 8. UI 규칙

- 메인 블루: cobalt/royal blue 계열, 현재 약 `#174FFF`
- 화이트 + 아주 연한 블루/그레이 fill
- 큰 타이포 + 넉넉한 여백
- 일본어 원문이 시각적 중심
- 정보 계층은 한국 제품 UI처럼 명확하게
- Duolingo처럼 과하게 게임화/귀엽게 만들지 않는다.
- 카드/패널/입력 같은 **버튼 외 컴포넌트에는 fill + border를 동시에 쓰지 않는다.**
- 버튼은 filled / outline 모두 허용한다.
- 카드 그리드 남발보다 row/list 구조를 선호한다.

## 9. 스크린샷 QA 원칙

사용자가 스크린샷으로 오류를 보내면:

1. 화면에서 실제로 무엇이 잘못됐는지 먼저 읽는다.
2. 단순 데이터 오류인지, 랭킹 오류인지, 발음 복원 오류인지, UI 오류인지 분리한다.
3. 비슷한 오류가 반복될 수 있으면 일반화된 수정으로 해결한다.
4. 수정 후 같은 입력 + 인접 케이스를 함께 점검한다.

예:
- `키미노 → キミの勇者`가 나온다면 “키미노” 한 건만 막는 대신 **짧은 표현 vs 긴 항목 랭킹 규칙**을 고친다.
- `도코에이코`가 깨지면 해당 문자열만 하드코딩하기보다 **붙여 쓴 구문 분절**을 고친다.

## 10. /log 규칙

사용자가 `/log`라고 하면 별도 확인 질문 없이 그날의 의미 있는 작업을 `bini-s2/Design-log`에 기록한다.

- 민감한 개인 정보는 기록하지 않는다.
- 기술/디자인/UX/AI/자동화/작업 결과 중심으로 정리한다.
- 완료 후 커밋 SHA를 알려준다.

## 11. 새 채팅에서 사용자가 이렇게 말하면

> Japanese Sound to Script 작업 이어서하자

바로 이 저장소의 `AGENTS.md`와 `docs/CHAT-CONTINUATION-WORKFLOW.md`를 확인한 뒤, 최근 GitHub 상태를 기준으로 작업을 이어간다. 사용자에게 과거 맥락을 다시 길게 설명하게 하지 않는다.
